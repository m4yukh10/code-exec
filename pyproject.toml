[project]
name = "docker-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mayukh sen", email = "mayukh.sen120@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "python-multipart>=0.0.32",
    "uvicorn>=0.52.4",
]

[project.scripts]
docker-py = "docker_py:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
